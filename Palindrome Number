class Solution:
    def isPalindrome(self, x: int) -> bool:
        sum=0
        final=x
        while (x!=0):
            num=int(x%10)
            sum=sum*10+num
            x=int(x/10)
        if final==sum:
            return True
        else:
            return False
        
