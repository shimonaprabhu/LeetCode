class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        nums=[]
        xor=0
        for i in range(n):
            nums.append(start+2*i)
        for i in range(len(nums)):
            xor=xor^nums[i]
        return xor
