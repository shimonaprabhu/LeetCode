class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        seen={}
        for i in range(len(nums)):
            seen[nums[i]]=True
        for i in range(len(nums)+1):
            if i not in seen:
                return i
                
       
